describe('Timer test integration', () => {
  // moge przestestować że jeżeli zostanie pobrane z bazy danych run, pause, lub coś innego czy dane komponenty się odpowiednio renderują, i czy napisy które zostały pograne też się renderują
  it('should', () => {});
});
